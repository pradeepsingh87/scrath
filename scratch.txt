# Use Java 11 base image
FROM openjdk:11-jre

# Set application version 
ENV APP_VERSION 1.0

# Copy the JAR file from the target folder
COPY target/myapp-$APP_VERSION.jar /opt/myapp.jar

# Expose port 8080 for Tomcat
EXPOSE 8080

# Run the JAR
ENTRYPOINT ["java", "-jar", "/opt/myapp.jar"]
